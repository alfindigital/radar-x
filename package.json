{
  "name": "radar-x",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "typecheck": "tsc --noEmit",
    "ingest": "tsx scripts/ingest.ts",
    "compute": "tsx scripts/compute.ts",
    "backfill": "tsx scripts/ingest.ts filings --months 6 && tsx scripts/ingest.ts index && tsx scripts/ingest.ts flows --limit 253 --only-missing && tsx scripts/ingest.ts prices --limit 253 --only-missing && tsx scripts/ingest.ts holders --limit 253 --only-missing && tsx scripts/ingest.ts broker --limit 40 --only-missing && tsx scripts/compute.ts"
  },
  "dependencies": {
    "next": "16.3.5",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "recharts": "^3.10.1",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.3.5",
    "tailwindcss": "^4",
    "tsx": "^4.23.13",
    "typescript": "^5"
  }
}
