# Sectors Financial API key — https://sectors.app/api (Insider)
SECTORS_API_KEY=
# Data backend: "json" (local files in ./data) or "supabase"
DATA_SOURCE=json
# Supabase (only when DATA_SOURCE=supabase)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
